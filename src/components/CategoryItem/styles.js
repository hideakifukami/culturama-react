import styled from "styled-components";

export const CategoryItem = styled.li`
    padding: 1.5rem;
    text-transform: uppercase;
    cursor: pointer;
    user-select: none;
    transition: 0.5s;
    opacity: 1;
    min-width: 282px;

    :hover {
        opacity: 0.7;
    }
`